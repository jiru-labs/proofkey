# Choosing a model

ProofKey runs on whatever you point it at, which makes "which model should I
use?" a real question with a real cost attached. This page answers it for
Gemini, and gives you the method for any other provider.

It follows the same discipline as [COMPATIBILITY.md](COMPATIBILITY.md): claims
carry their evidence. The cost half is **calculated** — arithmetic over prompt
sizes measured from the source. The effectiveness half is **measured**, for
three models so far, by a harness in the repo that anyone can run against
anything.

Do not read a cost ranking as a quality ranking. Measuring changed the
recommendation on this page: the cheapest model is not the one to use.

## Two workloads, different economics

ProofKey asks a model for two quite different things:

- **Live check** — fires about a second after you stop typing, batches up to 8
  changed sentences, and returns them corrected. High frequency, small
  payloads, mechanical task, latency you can feel.
- **Quick actions** — fix grammar, improve writing, summarize and the rest.
  One per invocation, larger input, more room for judgement, and you are
  waiting for it deliberately.

Live checking is where the money goes, because it repeats. Quick actions are
where quality shows. They do not have to run on the same model, and
[the settings support pinning them separately](#using-two-models).

## The numbers

Generated by `npm run cost` — the system-prompt sizes are measured from
`src/core/prompts.ts` rather than typed in, so this stays honest when the
prompts are edited.

### Token footprint

| Operation | Input tokens | Output tokens | What it is |
|---|---|---|---|
| Live check | 641 | 188 | one request, 8 sentences in and back |
| Fix grammar | 855 | 150 | one paragraph, rewritten at about the same length |
| Summarize | 451 | 45 | one paragraph in, a short summary out |

### Cost per 1,000 operations

| Model | Live check | Fix grammar | Summarize | Thinking |
|---|---|---|---|---|
| `gemini-2.5-flash-lite` | $0.14 | $0.15 | $0.06 | off via `reasoning_effort` |
| `gemini-3.1-flash-lite` | $0.44 | $0.44 | $0.18 | cannot be disabled |
| `gemini-2.5-flash` | $0.66 | $0.63 | $0.25 | off via `reasoning_effort` |
| `gemini-3.5-flash-lite` | $0.66 | $0.63 | $0.25 | cannot be disabled |
| `gemini-3.6-flash` | $2.37 | $2.41 | $1.01 | cannot be disabled |
| `gemini-3.5-flash` | $2.65 | $2.63 | $1.08 | cannot be disabled |
| `gemini-2.5-pro` | $2.68 | $2.57 | $1.01 | cannot be disabled |

**Assumptions.** 4 characters per token (Latin script — languages that tokenize
worse, like CJK, land nearer 2, so roughly double those figures);
90-character sentences, 8 per live-check request; a 600-character paragraph for
a quick action; an empty writing profile, since a style guide adds its own
length to *every* request.

Prices are USD per million tokens on the standard tier, checked **2026-08-01**
against [ai.google.dev/gemini-api/docs/pricing](https://ai.google.dev/gemini-api/docs/pricing).
**They change.** Re-check before trusting them, and regenerate with
`npm run cost -- --markdown`.

## What actually drives the number

### Output tokens dominate, so compare output prices

ProofKey rewrites text. The output is about as long as the input, and output is
priced 4–8× higher than input on every model above. So the output column is the
one that decides your bill — which is not how people usually skim a pricing
page.

That is why `gemini-2.5-flash-lite` is not marginally cheaper than the current
default but **~5× cheaper**: $0.40 versus $2.50 per million output tokens.

### Reasoning: less of a tax than expected

[Per Google's OpenAI-compatibility docs](https://ai.google.dev/gemini-api/docs/openai),
thinking **cannot be disabled** on Gemini 3.x models or on 2.5 Pro. On Gemini
2.5 Flash and Flash-Lite it can, with `reasoning_effort: "none"`. Since thinking
tokens are normally billed as output — the term that already dominates — the
obvious prediction is that a forced-reasoning model costs much more here.

**Measured, that did not happen.** Across three models on an identical
14-sentence request:

| Model | Thinking | `completion_tokens` |
|---|---|---|
| `gemini-2.5-flash-lite` | disabled | 196 |
| `gemini-2.5-flash` | disabled | 196 |
| `gemini-3.1-flash-lite` | **cannot be disabled** | 194 |

No inflation, and no `reasoning_tokens` field came back at all. Either the model
barely thinks on a task this mechanical, or the compatibility layer does not
report reasoning separately — the harness reads
`completion_tokens_details.reasoning_tokens` and found nothing to read.

So treat forced reasoning as a **latency and control** concern rather than a
billing one, until someone measures otherwise on a longer input. Setting
`reasoning_effort: "none"` where it is supported is still worth doing — it is
free, and it removes a variable.

### Turning thinking off

ProofKey merges **extra body fields** into the request root, so this needs no
code change. In the connection editor, add:

```json
{ "reasoning_effort": "none" }
```

Endpoints that do not know the field ignore it, so this is safe to leave set on
a connection you also point at other providers.

## The recommendation

Cost is calculated, quality is measured over three runs each:

| Use | Model | Why |
|---|---|---|
| **Live check** | `gemini-2.5-flash-lite` + `reasoning_effort: "none"` | 14/14 on every run, no false alarms, and the cheapest model on the page at $0.14 per 1,000 checks. Both the best and the cheapest, which is not how this usually goes. |
| **If latency matters more** | `gemini-3.1-flash-lite` | 250ms faster (918ms vs 1173ms), 13/14 and equally stable, at 3× the price. For as-you-type underlines that trade is worth considering. |
| **Quick actions** | `gemini-2.5-flash` + `reasoning_effort: "none"` | The preset default, and volume is low enough that price barely matters. Note this is **not** what the eval measures — see the caveat below. |
| **Avoid for live check** | 3.5-flash, 3.6-flash, 2.5-pro | 4–20× the cost on a task a flash-lite model does perfectly. |

**Caveat on the quick-actions row.** `npm run eval` exercises the live-check
prompt only. `gemini-2.5-flash` scores *worst* of the three there (11/14), but
that is a result about one prompt, not about the model's judgement on
"improve writing" or "make professional" — which nobody has measured. Do not
read the live-check ranking as a general ranking.

### Using two models

**Settings → Live checking → Connection.** Leave it on *Same as the active
connection* to keep one model for everything, or point it at a second, cheaper
connection and get the split above. Unset, it falls back to the active
connection (`src/background/index.ts:255`).

One difference worth knowing: quick actions fall through the fallback chain when
a provider fails, but live checking does not. A pinned connection is the only
one it tries, and a failed check is skipped rather than retried elsewhere. That
is deliberate — a silent retry storm on every keystroke pause is worse than a
missing underline — but it means the connection you pin here should be one you
expect to be up.

## The free tier

Gemini has a free tier, and for casual use it may cost you nothing at all.

Concrete free-tier limits are **not published as a table** any more — Google's
[rate limits page](https://ai.google.dev/gemini-api/docs/rate-limits) now points
you at [your own limits in AI Studio](https://aistudio.google.com/rate-limit),
because they vary by account. So this page deliberately quotes no numbers rather
than quoting stale ones.

Two things to weigh anyway:

- Live checking generates requests at a rate a request-per-day cap notices.
  Debounce and per-sentence caching keep it down, but it is the setting to turn
  off first if you hit a limit.
- Free-tier data handling differs from paid. Read Google's terms if the text you
  are checking is not yours to share.

## Measuring effectiveness

Nothing above tells you whether a model is actually *good* at this. That needs
measurement, and the harness is in the repo:

```bash
export PROOFKEY_EVAL_KEY=...          # a key for the endpoint you're testing
npm run eval                          # 3 runs each of gemini-2.5-flash-lite and -flash
npm run eval -- --models gemini-3.1-flash-lite --runs 5
npm run eval -- --base https://api.groq.com/openai/v1 --models llama-3.3-70b
```

It sends the **real** composed prompt from `src/core/prompts.ts` and parses the
reply with the **real** `parseCheckReply`, so what it measures is ProofKey
rather than an approximation. `runs × models` small requests — cents at most.

It reports five things, and only one of them is accuracy:

1. **Contract failures.** The numbered-line format is how a reply gets mapped
   back to sentences. A model that breaks it is unusable at any price, and small
   models break it most. This is the first thing to check on a cheap model.
2. **False alarms** — changing text that was already correct. Half the fixtures
   are clean text, because the prompt itself says an unnecessary change is worse
   than a missed error: the user reads it as the tool being wrong.
3. **Stability** — whether the same model gives the same answer twice. It
   often does not. The first version of this harness ran once per model and
   produced a number that moved by 3 of 14 on a re-run, which is how `--runs`
   came to exist. Fixtures that flip between runs are listed separately.
4. **Corrections made**, judged by exact match. Strict, so every distinct wrong
   answer is printed in full for a human to read — a model can be right in a way
   the fixture did not anticipate. **Do not quote a score without reading them**;
   the results above are a live demonstration of why.
5. **Real token counts** from the provider, reasoning tokens included where the
   provider reports them, as the honest check on `tools/cost.ts`.

The fixtures cover English, Spanish, French, German and mixed ES/EN, plus
register preservation (tú vs usted), URLs, mentions, hashtags, emoji, and one
prompt-injection attempt that must be treated as text to check rather than
instructions to follow.

### Results

Gemini, 14 fixtures × 3 runs, 2026-08-01, `npm run eval -- --runs 3`:

| Model | Correct | Spread | False alarms | Contract | Latency | Reported by |
|---|---|---|---|---|---|---|
| `gemini-2.5-flash-lite` | **14.0/14** | 14–14 | 0.0 | held 3/3 | 1173ms | maintainer |
| `gemini-3.1-flash-lite` | 13.0/14 | 13–13 | 0.0 | held 3/3 | **918ms** | maintainer |
| `gemini-2.5-flash` | 11.0/14 | 11–11 | 0.0 | held 3/3 | 1245ms | maintainer |

All three are now perfectly stable — identical score on every run, where the
worst of them previously ranged 9–13 on identical input. A model that answers
differently each time underlines a sentence, drops it, and underlines it again
as you type, so this matters as much as the score.

Only one failure survives in the two flash-lite models: "Their is alot of things
to do" comes back as "There **is** a lot of things to do" — the homophone and
the run-together word fixed, the subject-verb agreement missed. A missed error,
which is the acceptable kind. `gemini-2.5-flash` additionally inserts a comma
before Spanish *pero* in two fixtures, which is a house-style edit rather than a
correction.

### What these numbers cost to get

The first version of this table was much worse, and reading the mismatches is
what fixed it:

| | `2.5-flash-lite` | `3.1-flash-lite` | `2.5-flash` | False alarms |
|---|---|---|---|---|
| Original prompt | 10.3/14 (9–13) | 8.7/14 (8–9) | 8.0/14 (8–8) | 1.0 each |
| + messaging-conventions rules | 12.3/14 (9–14) | 13.0/14 | 12.7/14 | **0.0 each** |
| + worked example for pronoun *I* | **14.0/14** | 13.0/14 | 11.0/14 | 0.0 each |

Every mismatch in the first row was one of three things, and **none of them was
a proofreading failure**: adding a full stop to a line that had none, adding a
comma before Spanish *pero*, or flattening informal register. All three models
already fixed all 7 real errors and left 6 of 7 clean fixtures alone, including
the prompt-injection attempt, the URL with a query string, and the `usted`
register.

The first of those was the serious one. In chat — the only context ProofKey has
actually been verified in — people do not end messages with full stops, so live
checking would have underlined **almost every message** to suggest adding one:
the exact false-alarm firehose the live-check prompt is written to avoid. No
model choice fixes that. It was a prompt bug, and `composeCheckPrompt` now says
messaging conventions are not errors.

The second round is the more instructive one. That rule carried an exception —
the English pronoun *I* is still capitalised — and `gemini-3.1-flash-lite`
resolved the conflict the wrong way, leaving "i have been working" lowercase.
A regression introduced by the fix. Replacing the buried exception with a worked
example in the prompt (`4. i think its fine` → `4. I think it's fine`) settled
it. **Examples outrank rules**, at least here.

Two things to take from that beyond the numbers. The eval earned its cost by
finding a prompt bug that no amount of model-shopping would have solved. And
`gemini-2.5-flash` got *worse* between the last two rounds, from 12.7 to 11.0 —
prompt changes are not uniformly good across models, so a change that helps the
model you use can cost a contributor using another one.

The added rules grew the live-check prompt by about 120 tokens, roughly 24% more
input. Cost per 1,000 checks went from $0.13 to $0.14 — a 7% rise, because input
is the cheap side. Which is the "output dominates" point from the top of this
page, arriving from the other direction.

### Caveats

14 fixtures is a smoke test, not a benchmark: it will catch a model that is
unusable, not separate two good ones. Three runs is enough to expose instability
and not enough to rank. And add fixtures when you find a **real** failure — a
case a model got wrong in the wild is worth more than one somebody invented.

Post yours as a
[provider report](https://github.com/jiru-labs/proofkey/issues/new?template=provider-report.yml)
and it lands in the table.

## Other providers

The method transfers. For any provider:

1. Take the token footprint above — it is a property of ProofKey's prompts, not
   of the provider.
2. Multiply by that provider's prices, weighting **output** heavily.
3. Check whether reasoning can be switched off.
4. Run `npm run eval -- --base <url> --models <ids>` to find out whether it can
   hold the contract.

Steps 1–3 are arithmetic. Step 4 is the one that actually decides it.
